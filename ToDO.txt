===================================================================================
Rapport, AWS, refacto
PAS fait ou Sert à rien : finetuning (batch trop petit), LLM, TF idf, post processing, softlabels, fair voir try, warmpup, node2Vec, softpseudolabels, 
seuil adaptatif, combine self training GNN, threshold, combine embeddings, gnn + self training, consistency loss, circular reasoning (parler amélioration), 
hard labels vs softlabels, recheck labels -> bruit inutile, LLM API, differente simarité (cos sim etc), sentence-t5-xxl, recheck, agreement KFold
x = x + 0.02 * torch.randn_like(x) E5-large-v2


Long to test
Il y a toujours plein de choses à faire mais j'avais plus le temps (+ 100 submissions quand même)

BERT = comprendre les mots dans leur contexte
SBERT = représenter la phrase entière dans un vecteur sémantique

TF-IDF :

c’est juste du comptage de mots
pas de contexte
pas de synonymes
pas de compréhension
pas de structure
pas de paraphrases

Ils comprennent le sens, donc ils placent naturellement les catégories proches “près les unes des autres” dans l’espace vectoriel.

✔ SBERT ne comprend pas la hiérarchie
→ Il comprend seulement le sens.
✔ MAIS il positionne naturellement les concepts proches les uns des autres
→ ce que TF-IDF ne peut absolument pas faire.
✔ La hiérarchie explicite (parent/child)
→ c’est toi qui la rajoutes au-dessus des embeddings.

Test en anglais
Déjà finetuner
Récent

Tu lui donnes 531 catégories et :
il ne les sépare pas bien
il crée des clusters trop larges
il est mauvais sur long-tail
il surpondère des relations NLI trop fortes
SBERT-RoBERTa a été optimisé pour STS (phrase-phrase),
pas pour :
classification hiérarchique
milliers de comparaisons review → labels
embeddings propres entre centaines de classes

Preuve anti amazon : https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2
https://www.sbert.net/docs/sentence_transformer/pretrained_models.html
https://huggingface.co/sentence-transformers/all-distilroberta-v1
https://huggingface.co/optimum/all-MiniLM-L6-v2#:~:text=Table_title:%20Training%20data%20Table_content:%20header:%20%7C%20Dataset,%7C%20Number%20of%20training%20tuples:%20100%2C231%20%7C

attention use dropout

# Option 1: Silver MPNet → Train MiniLM (TON CHOIX)
Silver quality: ★★★★★ (meilleur modèle)
Training: ★★★★☆ (features ok)
→ Bon compromis qualité/vitesse

# Option 2: Silver MiniLM → Train MPNet (INVERSE)
Silver quality: ★★★☆☆ (labels moins précis)
Training: ★★★★★ (features riches)
→ Garbage in, garbage out! Labels médiocres limitent tout

# Option 3: Silver MPNet → Train MPNet (MÊME)
Silver quality: ★★★★★
Training: ★★★★★
→ Mais circular reasoning! Le MLP ne sert à rien
```

"Nos résultats révèlent un problème fondamental: la qualité des silver 
labels. Le GCN atteint 0.72 en validation mais performe moins bien sur 
Kaggle, tandis que l'InnerProduct Classifier atteint seulement 0.62 en 
validation mais 0.53 sur Kaggle. Cette inversion s'explique par le fait 
que les modèles plus expressifs (GCN, MLP) mémorisent les erreurs des 
silver labels, alors que l'InnerProduct Classifier, contraint par les 
embeddings sémantiques fixes, généralise mieux aux vrais labels."

# Input: LES MÊMES review_emb (MPNet frozen)
doc_proj = Linear(review_emb)  # LEARNABLE

# Class embeddings: DIFFÉRENTES (trainable + GCN)
class_refined = GCN(trainable_label_emb, A_hat)  # LEARNABLE

# Matching
logits = cosine(doc_proj, class_refined) * 30
loss = BCE(logits, silver_labels)

Raisons empiriques (les plus importantes):

✅ Meilleurs résultats Kaggle (preuve empirique > théorie)
✅ MPNet embeddings robustes (généralisent mieux)
✅ Moins d'overfitting sur silver labels bruités
✅ Préserve la sémantique pré-entraînée

Avec des silver labels BRUITÉS, le finetuning :

❌ amplifie les erreurs
❌ casse la sémantique MPNet
❌ crée du sur-ajustement
❌ fait baisser drastiquement F1 Kaggle

clean_name = class_name.replace('_', ' ')

le self training c surement bien mais là j'y arrivais pas mais prendre les silver labels les mieux puis finetuner avec puis entrainer le modèle avec finetuning bonne idée

┌─────────────────────────────────────────────┐
│  MPNet (Semantic)                           │
├─────────────────────────────────────────────┤
│  ✅ Comprend le sens (synonymes, contexte) │
│  ✅ "iPhone" ≈ "smartphone" ≈ "mobile"     │
│  ❌ Peut être trop abstrait parfois         │
│  ❌ Sensible au bruit dans texte           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  BM25 (Lexical)                             │
├─────────────────────────────────────────────┤
│  ✅ Matching exact de mots-clés            │
│  ✅ "laptop bag" trouve "laptop"           │
│  ✅ Robuste, simple, interprétable         │
│  ❌ Pas de compréhension sémantique        │
│  ❌ "phone" ≠ "smartphone" (diff mots)     │
└─────────────────────────────────────────────┘

mix silver good

0.52 sans rien 0.56 avec linear

Les silver labels ne sont pas utilisés dans une boucle d’auto-apprentissage.
Ils servent à :
initialiser ton modèle
donner du signal de structure (hiérarchie)
stabiliser les embeddings des classes
forcer un modèle léger (InnerProduct + BCE) à comprendre la position hiérarchique d’une classe
➡️ À ce stade, il n’y a pas de risque de feedback loop (puisque tu ne vas pas re-entraîner le teacher dessus plusieurs fois).

Si tu entraînes ton modèle avec :
3 labels verticaux → parfait
5 labels horizontaux (siblings) → incohérent
3 classes concurrentes → bruit
1 seul label → manque d’information hiérarchique

RAW = précision sémantique
HIERARCHICAL = cohérence structurelle

Raw embeddings (base_category_embeddings)

✅ Très précis - Chaque classe a son embedding spécifique
✅ Meilleur coverage - Peut distinguer entre classes similaires
❌ Moins de generalization - Si le review est ambigu, peut choisir une classe trop spécifique
Propagated embeddings (hierarchical_embeddings)

✅ Plus de generalization - Les embeddings des parents influencent les enfants
✅ Meilleure consistency - Respecte mieux la hiérarchie
❌ Moins précis - Les classes enfants deviennent plus similaires à leurs parents
❌ Peut perdre des nuances - Toutes les sous-catégories de "baby_food" deviennent trop similaires

faire commentaire trainable ou pas pour gnn

modèle trop rigide -> entrainement pas fou

✔ stabilité (moins de variance batch-to-batch)
✔ meilleur comportement sur les échantillons ambigus
✔ meilleure calibration (logits moins extrêmes)
✔ meilleure robustesse aux silver labels bruités
✔ convergence plus progressive → moins de sur-adaptation précoce

transformer légèrement les embeddings MPNet pour qu’ils collent mieux aux embeddings de labels
apprendre une séparation adaptée à ton dataset Amazon
faire de la calibration
generaliser sur les noisy silver labels
Mais ton backbone (MPNet) fait déjà l’essentiel du travail.
Ta job principale = améliorer les labels.
Le classifieur = un amplificateur de la qualité des labels.

evaluate threshold

# TON WORKFLOW:
# 1. Train sur silver labels (bruitées)
# 2. Prédis sur test avec ce modèle bruité
# 3. Ajoute ces prédictions bruitées au train
# 4. Re-train sur encore plus de bruit
# -> LE MODÈLE RENFORCE SES PROPRES ERREURS !

❌ 1. Tu n’as aucun vrai label

Tu entraînes sur du silver → bruit → fine-tuning = cataclysme.

❌ 2. Fine-tuning un modèle transformer nécessite :

labels non bruités
batchs de texte
learning rate tiny (~1e-5)
compute énorme
Impossible ici.

❌ 3. Fine-tuning détruirait la géométrie semantique

Et tout ton pipeline est basé sur la structure cosine de MPNet.

Si tu touches MPNet :
→ toutes tes distances changent
→ tes labels embed changent
→ ton pipeline s’effondre.

Ils ont déjà une géométrie :

hiérarchique
isotropique
cosiné stable
clusterisée par thèmes
Si tu défreeze :

→ tu détruis cette géométrie
→ tes catégories et tes reviews ne seront plus alignées
→ l’espace devient non-linéaire et chaotique
→ ton cosine-similarity basé pipeline ne fonctionne plus
→ ton classifieur se fait exploser

Threshold seul	Simple	Pas stable, dépend de la distribution
Quantile seul	Ajout contrôlé	Peut inclure des mauvais labels
Quantile + Threshold	⭐ Meilleure qualité + régularité	Paramétrage initial

parler mieux LLM
context enhancer

zero shot

reprendre intro paper
weakly
supervised hierarchical multi-label text classification

"We observe that when asked to assign
 multiple classes to a document, humans will first
 pinpoint most essential “core classes” and then
 check whether their ancestor classes in the taxon
omy should also be tagged."

Tes silver labels viennent d’un modèle S-BERT + propagation hiérarchique.
Ils sont ultra-précis, ultra-stables, et très rigides → ils représentent déjà 90% du signal final.
Tous tes modèles (MLP, InnerProduct, GCN, EMA…) apprennent juste :
👉 « Reproduire les labels qu’on m’a donnés ».

Donc :
plus le modèle est simple, mieux il generalise → car il colle aux silver labels, sans overfit ;
plus tu rajoutes de regularization, GCN, EMA, consistency, plus tu brouilles ce signal → perte de F1 ;
ton vrai score Kaggle dépend quasi uniquement de la qualité des silver labels, pas du classifier.

👉 TON PIPELINE EST 80% SILVER, 20% MODEL.


1️⃣ Dataless classification améliorée
encoder classes → embeddings
encoder documents → embeddings
prédire par similarité cosinus + GNN pour raffiner
C’est exactement une variante moderne du dataless classification.

2️⃣ Pseudo-labeling / Self-training
Tu ajoutes ensuite des pseudo-labels pour améliorer les représentations.
label_hierarchy_loss
macro f1 ou sample f1


✔ elle améliore la robustesse
✔ elle évite le sur-alignement des embeddings
✔ elle rend le modèle moins dépendant du bruit
✔ elle améliore la hiérarchie chain consistency
✔ elle évite que certains labels s'effondrent
✔ elle donne de meilleurs logits pour le ranking (P@1/P@3)

ajuster scale

===================================================================================

Pourquoi hier_norm est mieux même pour top-1 ?
Parce que le top-1 ne dépend pas uniquement de la proximité brute, mais de la structure des catégories :
Quand tu utilises hier_norm :
un enfant reste proche de son parent
deux enfants du même parent deviennent proches
les catégories orphelines deviennent cohérentes
Donc le top-1 devient :
✔ plus stable
✔ plus robuste
✔ plus cohérent
✔ plus “classifiable” pour ton modèle ensuite
✔ meilleur pour générer des silver labels hiérarchiques

comparer v1 et v2
skip conncection => - de Parent-Child Cosine Similarities 


Donc OUI, skip connections peuvent améliorer ton boost MAIS:

Ce n'est pas un effet direct - skip ne "crée" pas la structure parent-child
C'est un effet de préservation - skip empêche que tout devienne identique
Sans hierarchical loss, skip ne suffit pas - tu auras quand même boost ≈ 0

pas grand différence label embeddings hier
